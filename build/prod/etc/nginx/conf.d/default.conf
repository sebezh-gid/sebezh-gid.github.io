server {
    listen 80;

    index index.php;
    root /app/public;
    try_files $uri /index.php?$args;

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass 127.0.0.1:9000;
    }
}
